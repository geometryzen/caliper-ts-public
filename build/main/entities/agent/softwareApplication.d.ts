import { Entity } from '../entity';
/**
 * Represents SoftwareApplication.
 * SoftwareApplication's prototype set to Agent
 * @constructor
 * @param {string} id URI
 * @extends Organization
 */
export declare class SoftwareApplication extends Entity {
    constructor(id: string);
}

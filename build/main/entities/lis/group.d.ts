import { Organization } from '../agent/organization';
/**
 * Represents Group.
 * Group's prototype set to Organization
 * @constructor
 * @param {string} id URI
 * @extends Entity
 */
export declare class Group extends Organization {
    constructor(id: string);
}
